---
import { getLangFromUrl, useTranslations, translatePath } from '@/i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
---

<footer class="bg-gray-900 text-white">
  <div class="max-w-7xl mx-auto py-12 px-4 sm:px-6 lg:px-8">
    <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
      <div class="col-span-1 md:col-span-2">
        <h3 class="text-lg font-semibold mb-4">Casa del Jaguar</h3>
        <p class="text-gray-300 mb-4">
          {t('footer.description')}
        </p>
      </div>
      
      <div>
        <h4 class="text-md font-semibold mb-4">{t('footer.links')}</h4>
        <ul class="space-y-2">
          <li><a href={translatePath('/', lang)} class="text-gray-300 hover:text-white">{t('nav.home')}</a></li>
          <li><a href={translatePath('/about', lang)} class="text-gray-300 hover:text-white">{t('nav.about')}</a></li>
          <li><a href={translatePath('/services', lang)} class="text-gray-300 hover:text-white">{t('nav.services')}</a></li>
          <li><a href={translatePath('/contact', lang)} class="text-gray-300 hover:text-white">{t('nav.contact')}</a></li>
        </ul>
      </div>
      
      <div>
        <h4 class="text-md font-semibold mb-4">{t('footer.contact')}</h4>
        <p class="text-gray-300">Email: info@casadeljaguar.com</p>
        <p class="text-gray-300">Tel: +1 (555) 123-4567</p>
      </div>
    </div>
    
    <div class="mt-8 pt-8 border-t border-gray-800 text-center">
      <p class="text-gray-400">{t('footer.copyright')}</p>
    </div>
  </div>
</footer>
